// Explicit
let isAdmin: boolean = true;

// Implicit
let isUser = false;

// Boolean type এর variable এ আমাকে string বা number type এর data লিখতে দিবে না। Boolean ছাড়া অন্য কোন type এর ডাটা TS এখানে allow করে না।
// isAdmin = "Hello";
// isUser = 123;

console.log(isAdmin);
console.log(isUser);
